trigger: none
name: Terraform
pool: Default

stages:
- stage: A
  displayName: stage1
  pool: Default
  jobs: 
  - job: A
    displayName: Terraform init
    steps: 
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'SVB'
        scriptType: ps
        scriptLocation: inlineScript
        inlineScript: |
          az account show


